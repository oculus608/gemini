Act as a C# and UI expert. Design an implement a windows forms application for .NET 8.0 to manage a file parameters.json. You can see the file here: https://github.com/oculus608/gemini in the main branch. The code you provide will be compiled in VS 2022. Assume the application to be single-threaded, no need to be concerned about multi-threading issues. The application should be dark themed. That may requirent is that it should be as convinient to use for the end user as possible and look as nice and professional is possible. 

The VS solution and project already exist and you can look at them here: https://github.com/oculus608/ac-grid-generator-ui.git in the main branch. The code you generate will be placed in that solution.

Let's go through the data:
_Documentation_ should stay the way it is in file. User should not be able to change it. It documents the possible options for the GridStrategy. You can use it to display hints for it in UI. 

AcRootPath should not be part of UI and should be taken directly from the AppSettings.AssettoCorsaRoot.

PresetFolder should not be part of UI and should be taken directly from the AppSettings.Presets

PresetName can be any string. The user should be able to just type it in.

BaseSkill is a number between 70 and 100. Consider using a slider for it.

BaseAggression is a number between 0 and 100. Consider using a slider for it.

CarAllocations is a list of cars. The user should be able to add/remove/change cars. Please think how to structure the UI so that it is convinient for the user to do it. The user should not be see or work with CarId directly. The solution already contains a CarProvider that should be used to get an array of all the cars available to the user. The Car objects it returns contains Id, that is the one that should be in CarId in the json and the Name, that is what the user should work with. Please note that currently the list of cars is long: 218 cars. The user should be able to have a proper UI element that would help him selecting the cars. For example, a combination of textboxt+list to act as a smart combobox that would allow to type a part of the car name and then select from a filtered options, but you can come up with something better is you want.

Do not use JSON names directly as labels in the UI. Use human readable names. For example don't use BaseSkill. Use Base Skill instead. Don't use CarAllocations use Cars instead. And so on.

The path to parameters.json the app is managing should be taken from AppSettings.ParametersJson. If that file does not exist the app should create it and prepoulate with a Fixed strategy, skill 85, aggression 50 and no cars. Present name should be empty and folders should be taken from respective app settings. 

There is no need to have any save of load buttons. The app should be a UI for live managing that single json and any change that happens should be immeditely reflected in the file itself. Assume that no one is changing the file directly via text editing or any other way while the app is running.

Do not generate any code yet. Just tell me is the task is clear. And ask any questions you want on the matter.
